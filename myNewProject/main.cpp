/* This programs finds the average of a set of exam scores. */
#include <iostream> //Required for cin, cout

#include<cmath>
using namespace std;
int main()
{

  int n1(-1), count(0);
  if(n1>-4){
      count = ++count+n1;
      cout<<count;
  }
  cout<<count;
  }
     

    return 0;
}